# 🧩 Тестовое задание: Мини-платформа для публикации статей

Описание
Нужно разработать REST API для сервиса публикации статей. Пользователи могут регистрироваться, создавать статьи, ставить лайки и оставлять комментарии.

## ⚙️ Функциональные требования

### 1. Аутентификация и регистрация
- Регистрация нового пользователя (email, username, пароль).
- Авторизация через JWT-токен (использовать djangorestframework-simplejwt).
- Эндпоинт для получения и обновления профиля текущего пользователя.

### 2. Статьи
- Пользователь может:
  - Создать статью (заголовок, текст, теги через запятую, дата публикации).
  - Редактировать и удалять только свои статьи.
- Любой пользователь может:
  - Получить список всех статей.
  - Фильтровать статьи по тегам и автору.
  - Просмотреть конкретную статью по ID.
- Для каждой статьи возвращается:
  - Автор (username),
  - Количество лайков,
  - Количество комментариев.

### 3. Лайки
- Авторизованный пользователь может поставить/убрать лайк статье.
- Один пользователь может лайкнуть статью только один раз.

### 4. Комментарии
- Авторизованный пользователь может комментировать статьи.
- Комментарии можно удалять только автору комментария или автору статьи.
- У каждого комментария должны быть:
  - Автор,
  - Текст,
  - Дата создания.

## 💾 Модель данных (пример)

User
 ├─ id
 ├─ username
 ├─ email
 └─ password

Article
 ├─ id
 ├─ author (ForeignKey -> User)
 ├─ title
 ├─ content
 ├─ tags (CharField, например через запятую)
 ├─ created_at
 └─ updated_at

Like
 ├─ id
 ├─ user (ForeignKey -> User)
 └─ article (ForeignKey -> Article)

Comment
 ├─ id
 ├─ user (ForeignKey -> User)
 ├─ article (ForeignKey -> Article)
 ├─ text
 └─ created_at

## 🚀 Технические требования
- Django 4+, DRF.
- JWT-аутентификация (djangorestframework-simplejwt).
- Все эндпоинты в формате REST API (JSON).
- Использовать сериализаторы, валидацию и permissions DRF.
- Можно использовать SQLite.
- Минимум один кастомный permission.

### ✅ Будет плюсом
- Использование ViewSet и Router.
- Dockerfile / docker-compose.
